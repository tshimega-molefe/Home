import { FC } from "react"

interface ProductSectionProps {}

const ProductSection: FC<ProductSectionProps> = ({}) => {
  return (
    <section
      id="product-section"
      className="container relative w-full h-screen"
    >
      ProductSection
    </section>
  )
}

export default ProductSection
